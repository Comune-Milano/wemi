/** @format */

import { keyframes } from 'styled-components';

const move = keyframes`
  from {
    transform: translate(0px,-15px);
  }

  to {
    transform: translate(0px,15px);
  }
`;

export default move;
