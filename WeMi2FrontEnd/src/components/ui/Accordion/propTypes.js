/** @format */

import exact from 'prop-types-exact';
import PropTypes from 'prop-types';

const AccordionPropTypes = exact({
  AccordionHeader: PropTypes.func,
  AccordionBody: PropTypes.func,
});

export default AccordionPropTypes;
