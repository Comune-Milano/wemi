/** @format */

import React from 'react';
import { storiesOf } from '@storybook/react';
import Spaces from 'components/navigation/Spaces';

storiesOf('Spaces', module).add('Default', () => <Spaces />);
