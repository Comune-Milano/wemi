/**
 * Status code HTTP for success.
 */
export const SUCCESS = 200;

/**
 * Status code HTTP for server error.
 */
export const INTERNAL_SERVER_ERROR = 500;

/**
 * Status code HTTP for not found.
 */
export const NOT_FOUND = 404;

/**
 * Status code HTTP for forbidden.
 */
export const FORBIDDEN = 403;

/**
 * Status code HTTP for unauthorized.
 */
export const NOT_AUTHORIZED = 401;

/**
 * Status code HTTP for a malformed request.
 */
export const BAD_REQUEST = 400;